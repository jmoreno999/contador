  void _resetCounter() {
    setState(() {
      _counter = 0;
    });
  }

  FloatingActionButton(
              onPressed: _resetCounter,
              child: Text(
                'RESET',
                style: TextStyle(fontSize: 15),
              ),
            ),
            